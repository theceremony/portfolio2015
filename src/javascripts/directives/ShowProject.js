'use strict';

module.exports = function(){
	return function($scope,element,attrs) {
		// element.bind('click',function($event){
			//$event.preventDefault();
			//$scope.clickProject = $scope.getJobById(attrs['showpreview']);
			// console.log('Show Project', $scope.clickProject);
			// $scope.displayProjectById($scope.clickProject.id);
			//console.log('$scope',$scope);
		// })
	}
}